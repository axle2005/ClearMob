1.0.1
-Added Item's on Ground check to CommandExec. Currently only logs to console. 
-Switched a couple con.sendmessage to use Logger so it properly gets put in server logs. 
-Temp added Check for # of Zombies in world, might expand to print number of all entities to server log. 